#Karen Lizbeth Herrera Avila
def impar(n):
lista=int(input(lista de enteros))
enteros=[]
impares=[]
if n % 2 == 1:
    for n in lista: 
    enteros.append(n)
    sorted(enteros)
    impares=(sorted(enteros,reverse=True))
    print impares((sorted(enteros,reverse=True)[0]))